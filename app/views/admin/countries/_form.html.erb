  <%= form_for [:admin, @country] do |f| %>
    <%= f.label :name  %>
    <%= f.text_field :name %>

    <div class="cancel-submit">
      <%= link_to "Cancel", :back, class: 'btn btn-outline-light' %>
      <%= f.submit class: 'btn btn-outline-light' %>
    </div>
  <% end %>
